<% if request.image? %>
  <div class="image"><%= image_tag request.image %></div>
<% end %>

<div class="text"><%= request.text %></div>
<div class="description"><%= request.description %></div>

<% if request.completed_by? current_author %>
  <%= t 'tasuku.taskables.urls.responses.already_answered' %>
  <div class="response url">
    <% request.responses.by(current_user).each do |response| %>
      <%= embed_video(response, Tasuku.config.video_width, Tasuku.config.video_height) %>
    <% end %>
  </div>
<% else %>
  <div class="form">
    <%= render partial: 'tasuku/taskables/url/responses/form', locals: { request: request } %>
  </div>
<% end %>
