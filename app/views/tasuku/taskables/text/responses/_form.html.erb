<%= form_for request.responses.build, url: tasuku.taskables_text_request_responses_path(request_id: request.id), method: :post, html: { multipart: true } do |form| %>
  <%= form.text_area :text %>

  <%= form.submit t('tasuku.taskables.texts.responses.respond') %>
<% end %>
