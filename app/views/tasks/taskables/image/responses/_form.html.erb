<%= form_for request.responses.build, url: tasks.image_request_responses_path(request_id: request.id), method: :post, html: { multipart: true } do |form| %>
  <%= form.file_field :image %>

  <%= form.submit t('tasks.taskables.images.responses.upload') %>
<% end %>
